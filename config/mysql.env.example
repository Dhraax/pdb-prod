# Copia este fichero a config/mysql.env y pon valores propios.
# config/mysql.env esta en .gitignore y nunca se commitea.

MYSQL_ROOT_PASSWORD=cambia-esta-contrasena-de-root
MYSQL_DATABASE=pdb_prod
MYSQL_USER=pdb_prod
MYSQL_PASSWORD=cambia-esta-contrasena-de-usuario
CNR_EDITOR_MFA_ENCRYPTION_KEY=cambia-esta-clave-de-cifrado-mfa

# Oficios changelog

The CNR trade systems: recipes, stations, the alchemist's potions, the catalogue
and its migrations. Everything under `src/cnr/`, `migration/` and
`documentation/oficios/`.

One file per month, `YYYY-MM.md`, newest entry first inside each file. The rules
are in [`../README.md`](../README.md).
